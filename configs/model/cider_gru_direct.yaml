# CIDER-GRU one-shot: direct prediction without diffusion

backbone_type: cider_gru_direct

# Architecture (size configs may override these)
mlp_ratio: 4
dropout: 0.1

tau_min: 0.2
slot_init_scale: 0.5
